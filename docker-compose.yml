services:
  claw-email:
    image: 163clawmail:latest
    build: .
    container_name: claw-email
    restart: unless-stopped
    ports:
      - "3600:3600"
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json
